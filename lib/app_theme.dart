import 'package:flutter/material.dart';

const Color kPrimaryColor = Colors.blueAccent;
const Color kSecondaryColor = Colors.white;
const BorderRadius kRoundedRadius = BorderRadius.all(Radius.circular(20));
